# Быстрый старт - Token Sale Page

## Для локальной разработки

### Шаг 1: Клонируйте проект

```bash
git clone <your_repo_url>
cd token-sale-page
```

### Шаг 2: Установите зависимости

```bash
pnpm install
```

### Шаг 3: Настройте .env

```bash
cp .env.example .env
# Отредактируйте .env с вашими значениями
nano .env
```

Минимальные требуемые переменные:

```env
DATABASE_URL="mysql://user:password@localhost:3306/token_sale_db"
JWT_SECRET="your_secret_key"
VITE_APP_ID="your_app_id"
```

### Шаг 4: Создайте БД

```bash
# Если MySQL не запущен, запустите его
mysql -u root -p

# В MySQL консоли:
CREATE DATABASE token_sale_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'token_user'@'localhost' IDENTIFIED BY 'password';
GRANT ALL PRIVILEGES ON token_sale_db.* TO 'token_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

### Шаг 5: Запустите миграции

```bash
pnpm db:push
```

### Шаг 6: Запустите dev сервер

```bash
pnpm dev
```

Откройте браузер: http://localhost:5173

## Основные URL

- **Главная страница:** http://localhost:5173/
- **Страница продажи:** http://localhost:5173/token-sale
- **API:** http://localhost:3000/api/trpc

## Команды для разработки

```bash
# Запуск в режиме разработки
pnpm dev

# Проверка типов
pnpm check

# Форматирование кода
pnpm format

# Запуск тестов
pnpm test

# Сборка для продакшена
pnpm build

# Запуск собранного приложения
pnpm start

# Миграции БД
pnpm db:push
```

## Структура файлов для редактирования

### Главная страница продажи
**Файл:** `client/src/pages/TokenSale.tsx`

Здесь находится весь UI для покупки токенов. Можно менять:
- Цвета и дизайн
- Текст и описания
- Логику валидации
- Способы оплаты

### Конфигурация
**Файл:** `src/web-utils/constants.ts`

Здесь находятся все константы:
```typescript
const tokenAddress = '0xBfF629448eE52e8AfB6dAEe47b64838228Bc5667';
const tokenPrice = 0.01; // $0.01
const receiverWallet = '0xf1829111dce451f62a3f0267bc1ed05328c03360';
```

### Функции для работы со смарт-контрактом
**Файл:** `src/web-utils/contract_functions.ts`

Здесь находятся функции для:
- Отправки BNB платежей
- Отправки USDC платежей
- Получения баланса
- Взаимодействия с токеном

## Подключение кошелька

Приложение поддерживает:
- **MetaMask** - основной способ
- **WalletConnect** - альтернативный способ (требует настройки)

Для WalletConnect нужно получить Project ID на https://cloud.walletconnect.com

## Тестирование

### Локально (без блокчейна)

```bash
# Запустите тесты
pnpm test

# Смотрите результаты
# Должно пройти 14 тестов
```

### На тестовой сети (BSC Testnet)

1. Переключитесь на BSC Testnet в MetaMask
2. Получите тестовые BNB: https://testnet.binance.org/faucet-smart
3. Измените в `constants.ts`:
```typescript
const baseChainId = 97; // BSC Testnet вместо 56
const chainRPC = 'https://data-seed-prebsc-1-s3.binance.org:8545';
```
4. Разверните тестовый токен на Testnet
5. Тестируйте покупку

## Решение проблем

### Ошибка "Cannot find module"
```bash
# Переустановите зависимости
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

### Ошибка подключения к БД
```bash
# Проверьте, что MySQL запущен
mysql -u root -p

# Проверьте DATABASE_URL в .env
# Формат: mysql://user:password@host:port/database
```

### Ошибка с портом 3000
```bash
# Порт занят другим приложением
# Найдите процесс
lsof -i :3000

# Убейте процесс
kill -9 <PID>
```

### Кошелек не подключается
1. Убедитесь, что установлен MetaMask
2. Проверьте, что вы на правильной сети (BSC Mainnet или Testnet)
3. Откройте консоль браузера (F12) и посмотрите ошибки
4. Перезагрузите страницу

## Развертывание на VPS

Когда будете готовы к развертыванию на Hostinger, следуйте инструкциям в **DEPLOYMENT_GUIDE.md**

## Важные файлы

| Файл | Назначение |
|------|-----------|
| `client/src/pages/TokenSale.tsx` | Главная страница продажи |
| `src/web-utils/constants.ts` | Конфигурация (адреса, цены) |
| `src/web-utils/contract_functions.ts` | Функции для работы с контрактом |
| `server/routers.ts` | API endpoints |
| `drizzle/schema.ts` | Схема БД |
| `.env` | Переменные окружения |

## Следующие шаги

1. **Протестируйте локально** - убедитесь, что все работает
2. **Настройте смарт-контракт** - решите механизм продажи
3. **Разверните на VPS** - используйте DEPLOYMENT_GUIDE.md
4. **Настройте домен** - подключите свой домен
5. **Запустите на продакшене** - используйте PM2 или systemd

## Поддержка

Если возникнут вопросы:
1. Проверьте логи: `pnpm dev` выведет ошибки в консоль
2. Посмотрите консоль браузера (F12)
3. Проверьте переменные окружения в `.env`
4. Убедитесь, что БД работает: `mysql -u token_user -p token_sale_db`

---

**Готово к разработке!** 🚀
